{
  "name": "ha-heatmap-continued",
  "private": true,
  "description": "No build step required. Edit heatmap-card.js directly and deploy to Home Assistant.",
  "scripts": {
    "test": "TZ=UTC node --test test/"
  }
}
